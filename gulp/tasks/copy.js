/* src() - получает доступ к файлам и папкам по указанному пути 
    .pipe() - действие
    dest() - вывод результирующего файла в папку назначения (dest – пункт назначения).
*/

// export const copy = () => {
//     return app.gulp.src(app.path.src) 
//     .pipe(app.gulp.dest(app.path.build))
// }